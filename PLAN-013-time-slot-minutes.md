# Feature Implementation Plan — 时间区间支持分钟

**Overall Progress:** `100%`

## TLDR

时间区间由「仅整点」扩展为「时+分」：配置与 UI 支持 `startMinute`/`endMinute`（0–59），小时仅 0–23；止 23:59 表示「到当日结束」（比较时视为 24:00 独占上界）。左闭右开、跨天语义不变；兼容旧配置（无分钟视为 0，旧 endHour=24 转为 23:59）。

## Critical Decisions

- **小时 0–23，不出现 24**：配置与 UI 一律 0–23。「到当日结束」用止 23:59 表示；比较时将该 end 当作独占上界 1440 分钟。
- **Dashboard 方案 A**：每行四个数字框 — 起（时、分）、止（时、分），时 0–23，分 0–59。
- **精度只到分钟**：不支持秒。
- **向后兼容**：缺省 `startMinute`/`endMinute` 视为 0；旧配置中 `endHour=24` 在 merge/归一化时转为 `endHour=23, endMinute=59`。

## Tasks

- [x] 🟩 **Step 1: Schedule 类型、校验与区间逻辑（schedule.ts）**
  - [x] 🟩 `TimeSlot` 增加 `startMinute`、`endMinute`（0–59）；注释与类型约定小时 0–23（止 23:59 表示到当日结束）
  - [x] 🟩 `validateTimeSlot`：start/end 的 hour 0–23、minute 0–59
  - [x] 🟩 `getDefaultSchedule()` 的 timeSlots 带 `startMinute:0, endMinute:0`；默认区间 end 23:59 表示到当日结束
  - [x] 🟩 归一化：`normalizeTimeSlot` 在 merge 时对每个 slot 缺分钟补 0、`endHour===24` 改为 23:59
  - [x] 🟩 新增内部 helper：`slotStartMinutes` / `slotEndMinutesExclusive`（止 23:59 视为 1440）；跨天即 endM < startM
  - [x] 🟩 `getIndustryForNow()` 用当前时+分换算为分钟数，与各 slot 起止比较（左闭右开）

- [x] 🟩 **Step 2: Dashboard 时间区间 UI（server.ts）**
  - [x] 🟩 `renderTimeSlots`：每行四个数字框 — 起时、起分、止时、止分（min/max 0–23、0–59），保留行业下拉与删除按钮
  - [x] 🟩 `syncTimeSlotsFromDOM`：从 DOM 读四个输入写回 `slot.startHour/startMinute/endHour/endMinute`，小时/分 clamp 0–23、0–59
  - [x] 🟩 `collectSchedule` 中 timeSlots 收集四字段；btnAddSlot 默认 `startMinute:0, endMinute:0`，end 1:0
  - [x] 🟩 回填与校验：非法时起默认 0、止默认 23/59

- [x] 🟩 **Step 3: 文档与示例**
  - [x] 🟩 `schedule.example.json`：第一个 timeSlot 带 `startMinute`/`endMinute` 示例（其余仅整点展示兼容）
  - [x] 🟩 README 中 timeSlots 说明补充：支持 `startMinute`/`endMinute`（0–59），小时 0–23，止 23:59 表示到当日结束
